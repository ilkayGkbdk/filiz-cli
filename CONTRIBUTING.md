# Contributing

Filiz is an early private project. Coordinate changes with the repository owner
before starting substantial work.

## Local checks

Use stable Rust and run these commands before submitting a change:

```sh
cargo fmt --check
cargo check
cargo test
```

Keep changes focused, document user-visible behavior, and avoid committing local
environment files, credentials, build output, or editor settings.
